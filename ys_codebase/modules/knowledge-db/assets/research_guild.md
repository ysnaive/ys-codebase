- **調研知識庫預檢指引**：在啟動技術調研 (Research) 前，優先以複合關鍵詞或語意敘述執行 `python __${yscb.host://yscb.py}__ knowledge-db search <關鍵字> -s` 檢索既有架構設計與模組能力；涉及上下游架構依賴探索時，調用 `knowledge-db callers`、`callees` 與 `impact` 繪製調用拓撲，避免重複發明輪子。

